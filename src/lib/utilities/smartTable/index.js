import buildColumns from './buildColumns'
import { setUpFilters, readAppliedSorter, applySorter, applyPagination, applyFilters } from './filtering'

export default {
  buildColumns,
  setUpFilters,
  readAppliedSorter,
  applySorter,
  applyPagination,
  applyFilters
}
